#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
int main(void)
{
	char s[] = {'4', '3', '0', '7', '1',}
	char *p;

	p = leet(s);
	rintf("%s", p);
	printf("%s", s);
	return (0);
}
